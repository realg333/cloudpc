---
phase: 02-payments-order-automation
plan: 01
type: execute
wave: 1
depends_on: [02-00]
files_modified:
  - prisma/schema.prisma
  - prisma/seed.ts
  - src/app/api/orders/route.ts
  - src/lib/plans/catalog.ts
  - src/lib/payments/gateway.ts
  - src/lib/payments/mock-gateway.ts
  - src/lib/orders/concurrency.ts
autonomous: true
requirements: [PLAN-03, PAY-03]
user_setup: []

must_haves:
  truths:
    - "Payment and PaymentLog models exist with correct schema"
    - "Order has amountCents and currency; Plan has priceCents"
    - "hasActiveOrder correctly identifies users with paid orders"
    - "PaymentGateway interface has mock adapter for development"
  artifacts:
    - path: prisma/schema.prisma
      provides: "Payment, PaymentLog models; Order.amountCents; Plan.priceCents"
    - path: src/lib/orders/concurrency.ts
      provides: "hasActiveOrder(userId) for PLAN-03"
    - path: src/lib/payments/gateway.ts
      provides: "PaymentGateway interface"
    - path: src/lib/payments/mock-gateway.ts
      provides: "Mock adapter for testing/development"
  key_links:
    - from: src/app/api/orders/route.ts
      to: prisma
      via: "Order create with amountCents from Plan.priceCents"
      pattern: "amountCents|priceCents"
---

<objective>
Extend schema with Payment, PaymentLog, pricing fields; ensure concurrency module is complete; add PaymentGateway interface and mock adapter for development.

Purpose: Foundation for webhook handler and payment intent API in Plan 02.
Output: Migration, updated seed, order creation with amountCents, mock gateway adapter.
</objective>

<execution_context>
@C:/Users/realg/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/realg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-payments-order-automation/02-CONTEXT.md
@.planning/phases/02-payments-order-automation/02-RESEARCH.md
@prisma/schema.prisma
@src/app/api/orders/route.ts
@src/lib/plans/catalog.ts
@prisma/seed.ts
</context>

<interfaces>
<!-- From prisma/schema.prisma - Order model -->
model Order {
  id, userId, planId, machineProfileId, status, createdAt, updatedAt
  // Plan 01 adds: amountCents, currency
}

<!-- From 02-RESEARCH.md - Payment model -->
model Payment {
  id, orderId, gatewayPaymentId, amountCents, currency, method, status, createdAt, updatedAt
}

<!-- From 02-RESEARCH.md - PaymentLog model -->
model PaymentLog {
  id, gatewayEventId @unique, eventType, orderId, outcome, processedAt, metadata
}
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Schema and seed for payments and pricing</name>
  <files>prisma/schema.prisma, prisma/seed.ts</files>
  <read_first>
    - prisma/schema.prisma (current Order, Plan, MachineProfile models)
    - prisma/seed.ts (Plan upsert structure)
    - .planning/phases/02-payments-order-automation/02-RESEARCH.md (Schema Recommendations)
  </read_first>
  <action>
Extend prisma/schema.prisma:

1. Add to Plan model: priceCents Int @default(0)
2. Add to Order model: amountCents Int?, currency String? @default("BRL"), payments Payment[]
3. Add Payment model:
   model Payment {
     id               String   @id @default(cuid())
     orderId          String   @unique
     order            Order    @relation(fields: [orderId], references: [id], onDelete: Cascade)
     gatewayPaymentId String
     amountCents      Int
     currency         String   @default("BRL")
     method           String
     status           String
     createdAt        DateTime @default(now())
     updatedAt        DateTime @updatedAt
     @@index([gatewayPaymentId])
   }
4. Add PaymentLog model:
   model PaymentLog {
     id              String   @id @default(cuid())
     gatewayEventId  String   @unique
     eventType       String
     orderId         String?
     outcome         String
     processedAt     DateTime @default(now())
     metadata        Json?
     @@index([orderId])
   }

Update prisma/seed.ts:
- Add priceCents to each Plan upsert: Pacote 4h = 1500, Pacote 24h = 4500, Pacote semanal = 12000 (cents, i.e. R$15, R$45, R$120)
- Use update: { ..., priceCents: 1500 } and create: { ..., priceCents: 1500 } for each plan

Run: npx prisma migrate dev --name add_payments_pricing
  </action>
  <verify>
    <automated>npx prisma validate</automated>
  </verify>
  <acceptance_criteria>
    - prisma/schema.prisma contains model Payment with orderId, gatewayPaymentId, amountCents, method, status
    - prisma/schema.prisma contains model PaymentLog with gatewayEventId @unique
    - prisma/schema.prisma Plan model has priceCents Int
    - prisma/schema.prisma Order model has amountCents Int?, currency String?, payments Payment[]
    - prisma/seed.ts Plan upserts include priceCents: 1500, 4500, 12000 for 4h, 24h, semanal
    - npx prisma validate exits 0
  </acceptance_criteria>
  <done>Schema extended; migration created; seed includes priceCents</done>
</task>

<task type="auto">
  <name>Task 2: Order creation with amountCents and catalog priceCents</name>
  <files>src/app/api/orders/route.ts, src/lib/plans/catalog.ts</files>
  <read_first>
    - src/app/api/orders/route.ts (POST handler, plan/machineProfile fetch)
    - src/lib/plans/catalog.ts (PlanOption interface, listPlanOptions)
    - prisma/schema.prisma (Plan.priceCents, Order.amountCents)
  </read_first>
  <action>
Update src/app/api/orders/route.ts POST handler:
- After validating plan and machineProfile exist, read plan.priceCents (plan must include priceCents from Prisma)
- In prisma.order.create data, add: amountCents: plan.priceCents, currency: 'BRL'
- Ensure plan fetch uses: prisma.plan.findFirst({ where: { id: planId, isActive: true } }) — Prisma returns all scalar fields including priceCents

Update src/lib/plans/catalog.ts:
- Add priceCents: number to PlanOption interface
- In listPlanOptions, add priceCents: plan.priceCents to each option
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/app/api/orders/route.ts prisma.order.create includes amountCents: plan.priceCents and currency: 'BRL'
    - src/lib/plans/catalog.ts PlanOption has priceCents; listPlanOptions maps plan.priceCents
    - grep -n "amountCents\|priceCents" src/app/api/orders/route.ts returns matches
    - npm run build exits 0
  </acceptance_criteria>
  <done>New orders have amountCents from plan; catalog exposes priceCents</done>
</task>

<task type="auto">
  <name>Task 3: Implement hasActiveOrder for PLAN-03</name>
  <files>src/lib/orders/concurrency.ts</files>
  <read_first>
    - src/lib/orders/concurrency.ts (stub from Plan 00)
    - prisma/schema.prisma (Order model, status field)
    - .planning/phases/02-payments-order-automation/02-RESEARCH.md (Pattern 3: PLAN-03 Concurrency Check)
  </read_first>
  <action>
Replace the stub in src/lib/orders/concurrency.ts with real implementation:

- Import prisma from '@/lib/db'
- Implement hasActiveOrder(userId: string): Promise<boolean>:
  - const paid = await prisma.order.findFirst({ where: { userId, status: 'paid' } })
  - return !!paid
- Remove any stub return false; this is the real PLAN-03 check used by payment intent API and webhook handler
  </action>
  <verify>
    <automated>npm run test -- src/lib/orders/concurrency.test.ts --run</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/orders/concurrency.ts imports prisma from '@/lib/db'
    - hasActiveOrder uses prisma.order.findFirst with where: { userId, status: 'paid' }
    - hasActiveOrder returns true when user has paid order, false otherwise
    - npm run test -- src/lib/orders/concurrency.test.ts exits 0
  </acceptance_criteria>
  <done>hasActiveOrder correctly identifies users with paid orders for PLAN-03</done>
</task>

<task type="auto">
  <name>Task 4: PaymentGateway mock adapter</name>
  <files>src/lib/payments/gateway.ts, src/lib/payments/mock-gateway.ts</files>
  <read_first>
    - src/lib/payments/gateway.ts (from Plan 00 — interface and types)
    - .planning/phases/02-payments-order-automation/02-RESEARCH.md (Pattern 1: Gateway-Agnostic Interface)
  </read_first>
  <action>
Ensure src/lib/payments/gateway.ts has (add if Plan 00 did not create):
- export interface CreatePaymentIntentParams { orderId: string; amountCents: number; currency: string; method: 'pix' | 'crypto'; metadata?: Record<string, string> }
- export interface CreatePaymentIntentResult { paymentId: string; qrCode?: string; redirectUrl?: string }
- export interface WebhookParsed { eventId: string; eventType: string; orderId: string; status: 'paid' | 'failed' | 'expired' }
- export interface PaymentGateway {
  createPaymentIntent(params: CreatePaymentIntentParams): Promise<CreatePaymentIntentResult>;
  verifyWebhookSignature(payload: string, headers: Record<string, string>): boolean;
  parseWebhookPayload(body: unknown): WebhookParsed | null;
}

Create src/lib/payments/mock-gateway.ts:
- Import PaymentGateway, CreatePaymentIntentParams, CreatePaymentIntentResult, WebhookParsed
- Export createMockGateway(): PaymentGateway that:
  - createPaymentIntent: returns { paymentId: `mock_${Date.now()}`, qrCode: 'mock-qr-code', redirectUrl: undefined }
  - verifyWebhookSignature: returns headers['x-test-signature'] === 'valid' for testing; otherwise false (or use process.env.PAYMENT_WEBHOOK_SECRET with HMAC if set)
  - parseWebhookPayload: if body is object with id, event, data.orderId, data.status, return { eventId: body.id, eventType: body.event, orderId: body.data.orderId, status: body.data.status }; else null
- Mock is for development and tests; real gateway adapter will be added when gateway is chosen
  </action>
  <verify>
    <automated>npm run test -- src/lib/payments/gateway.test.ts --run</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/payments/gateway.ts exports PaymentGateway, CreatePaymentIntentParams, CreatePaymentIntentResult, WebhookParsed
    - src/lib/payments/mock-gateway.ts exports createMockGateway returning PaymentGateway
    - createMockGateway().createPaymentIntent returns object with paymentId and qrCode
    - createMockGateway().parseWebhookPayload accepts { id, event, data: { orderId, status } } and returns WebhookParsed
    - npm run test -- src/lib/payments/gateway.test.ts exits 0
  </acceptance_criteria>
  <done>Mock gateway adapter exists for development and tests</done>
</task>

</tasks>

<verification>
- npx prisma validate passes
- npm run build passes
- npm run test -- src/lib/orders/concurrency.test.ts src/lib/payments/gateway.test.ts passes
</verification>

<success_criteria>
- Payment and PaymentLog models in schema with correct relations
- Plan.priceCents and Order.amountCents populated; new orders get amountCents from plan
- hasActiveOrder (from Plan 00) works with Order.status='paid'
- Mock gateway adapter implements PaymentGateway interface
</success_criteria>

<output>
After completion, create .planning/phases/02-payments-order-automation/02-01-SUMMARY.md
</output>
