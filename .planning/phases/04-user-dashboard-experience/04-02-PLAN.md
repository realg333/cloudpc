---
phase: 04-user-dashboard-experience
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/app/api/dashboard/vms/[id]/connection/route.ts
  - src/components/VmStatusCard.tsx
  - src/app/orders/page.tsx
autonomous: true
requirements:
  - DASH-03
  - DASH-04
  - DASH-05

must_haves:
  truths:
    - "Connect button launches Parsec connection flow or copies connection details for ready VMs"
    - "User sees VM state; placeholder for future start/stop API"
    - "User can view order history with basic usage records (start/end timestamps)"
  artifacts:
    - path: src/app/api/dashboard/vms/[id]/connection/route.ts
      provides: "Secure connection details API"
      exports: ["GET"]
    - path: src/components/VmStatusCard.tsx
      provides: "Connect button, state display, start/stop placeholder"
    - path: src/app/orders/page.tsx
      provides: "Order history with provisionedVm usage"
  key_links:
    - from: src/components/VmStatusCard.tsx
      to: /api/dashboard/vms/[id]/connection
      via: "Connect button fetches and copies connection details"
      pattern: "connection|connect"
    - from: src/app/orders/page.tsx
      to: prisma.provisionedVm
      via: "include provisionedVm in order query"
      pattern: "provisionedVm|include"
---

<objective>
Add Connect button for Parsec, start/stop state placeholder, and order history with usage records.
</objective>

<execution_context>
@C:/Users/realg/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/realg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-user-dashboard-experience/04-CONTEXT.md
@prisma/schema.prisma
@src/components/VmStatusCard.tsx
@src/app/orders/page.tsx
</context>

<interfaces>
From 04-CONTEXT.md:
  - connectionMethod='parsec', connectionState='ready' when vm_ready
  - Connect button must launch Parsec or copy connection details
  - Credentials exposed securely via API (no plaintext in HTML)

From ProvisionedVm: ipAddress, hostname, connectionMetadata (Json), windowsUsername

Parsec connection: No parsec:// URL protocol. User connects via Parsec app using peer_id or by finding computer. For MVP: copy IP + hostname to clipboard; user pastes into Parsec or finds computer in app.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Connection API and Connect button</name>
  <files>src/app/api/dashboard/vms/[id]/connection/route.ts, src/components/VmStatusCard.tsx</files>
  <read_first>
    - src/app/api/dashboard/vms/route.ts
    - src/components/VmStatusCard.tsx
    - prisma/schema.prisma
  </read_first>
  <action>
    1. Create GET /api/dashboard/vms/[id]/connection:
       - getSessionFromCookies(); 401 if unauthenticated
       - Find ProvisionedVm by id where order.userId = session.user.id (ownership check)
       - If not found or not owner: 404
       - If connectionState !== 'ready' or status !== 'vm_ready': 400 "VM not ready for connection"
       - Return JSON: { ipAddress, hostname, connectionMethod, peerId?: connectionMetadata?.peerId }
       - Do NOT return windowsUsername or password in response (Parsec uses peer_id or host discovery)
    2. Update VmStatusCard: when status === 'vm_ready' and connectionState === 'ready', add "Conectar" button (client component or wrap in client)
       - On click: fetch GET /api/dashboard/vms/[id]/connection
       - Copy to clipboard: hostname or ipAddress (primary), with fallback. Format: "Host: {hostname}\nIP: {ipAddress}" or similar for user to paste into Parsec
       - Show toast or inline message "Copiado! Abra o Parsec e conecte usando o endereço."
       - Link to https://parsec.app/downloads if user needs to install Parsec
    3. If connectionMetadata contains peerId: prefer showing/copying peerId (Parsec command: parsecd peer_id=XXX)
  </action>
  <verify>
    <automated>curl -s -b "session=..." http://localhost:3000/api/dashboard/vms/VALID_VM_ID/connection returns 200 with ipAddress when VM is vm_ready and user owns it</automated>
  </verify>
  <acceptance_criteria>
    - GET /api/dashboard/vms/[id]/connection exists, requires auth, checks ownership
    - Returns 400 when VM not ready
    - Connect button visible when vm_ready + connectionState=ready
    - Click copies connection details to clipboard
  </acceptance_criteria>
  <done>Connect button copies connection details; user can paste into Parsec to connect</done>
</task>

<task type="auto">
  <name>Task 2: Start/stop state and placeholder</name>
  <files>src/components/VmStatusCard.tsx</files>
  <read_first>
    - src/components/VmStatusCard.tsx
    - .planning/phases/04-user-dashboard-experience/04-CONTEXT.md
  </read_first>
  <action>
    Per rules: Phase 3 uses expiry-based teardown; Vultr instances run until expiry. Implement "show state" and placeholder for future start/stop API.
    1. In VmStatusCard, when status is vm_ready or expiring: show "Estado: Rodando" (or "Ativo")
    2. Add a disabled "Parar" button with title/tooltip "Em breve — o tempo continua contando até o fim do período"
    3. No API call for start/stop — Vultr does not support pause/resume for GPU instances in this flow; teardown is expiry-based
    4. Optional: Add GET /api/dashboard/vms/[id]/state stub that returns { status } — for future start/stop API wiring
  </action>
  <verify>
    <automated>grep -r "Estado: Rodando\|Em breve" src/components/VmStatusCard.tsx</automated>
  </verify>
  <acceptance_criteria>
    - VmStatusCard shows "Estado: Rodando" for vm_ready/expiring
    - Disabled "Parar" button with explanatory tooltip
    - No functional start/stop API (placeholder only)
  </acceptance_criteria>
  <done>User sees VM state; placeholder indicates future start/stop capability</done>
</task>

<task type="auto">
  <name>Task 3: Order history with usage records</name>
  <files>src/app/orders/page.tsx</files>
  <read_first>
    - src/app/orders/page.tsx
    - prisma/schema.prisma
  </read_first>
  <action>
    Extend orders page to satisfy DASH-05 (order history and usage records):
    1. Update prisma.order.findMany to include provisionedVm: true
    2. In the orders table, add column or expand row to show:
       - If order has provisionedVm: "Máquina: {statusLabel} — Iniciado em {readyAt locale}, Encerrado em {destroyedAt or expiresAt locale}"
       - If no provisionedVm but order paid: "Provisionando..."
       - Format dates: toLocaleDateString('pt-BR', { day, month, year, hour, minute })
    3. Ensure orders are ordered by createdAt desc (already done)
    4. Add link from dashboard to /orders: "Ver histórico de pedidos"
    5. Usage records = readyAt (start), destroyedAt or expiresAt (end) — show these timestamps for completed/destroyed VMs
  </action>
  <verify>
    <automated>grep -E "provisionedVm|readyAt|destroyedAt" src/app/orders/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Orders query includes provisionedVm
    - Orders page shows machine status and usage timestamps (start/end) for each order with VM
    - Dashboard links to /orders for history
  </acceptance_criteria>
  <done>User can view order history with basic usage records (start/end timestamps)</done>
</task>

</tasks>

<verification>
- Run: npm run build — must pass
- Manual: Connect button copies details; Parsec connection works with pasted info
- Manual: Orders page shows usage timestamps for past VMs
</verification>

<success_criteria>
1. Connect button launches/copies Parsec connection flow for ready VMs
2. VM state displayed; placeholder for future start/stop
3. Order history shows usage records (start/end timestamps)
</success_criteria>

<output>
After completion, create .planning/phases/04-user-dashboard-experience/04-02-SUMMARY.md
</output>
