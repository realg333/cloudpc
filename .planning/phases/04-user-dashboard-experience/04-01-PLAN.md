---
phase: 04-user-dashboard-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/dashboard/page.tsx
  - src/app/api/dashboard/vms/route.ts
  - src/components/VmStatusCard.tsx
  - src/components/DashboardVmList.tsx
autonomous: true
requirements:
  - DASH-01
  - DASH-02

must_haves:
  truths:
    - "User can see list of active and recent VMs on dashboard"
    - "User can see VM status (provisioning, vm_ready, expiring, destroying, destroyed, failed)"
    - "User can see remaining time for active VMs"
    - "VM status updates without manual refresh when provisioning completes"
  artifacts:
    - path: src/app/dashboard/page.tsx
      provides: "Dashboard shell with VM list"
      min_lines: 40
    - path: src/app/api/dashboard/vms/route.ts
      provides: "VM list API for current user"
      exports: ["GET"]
    - path: src/components/VmStatusCard.tsx
      provides: "VM card with status and remaining time"
      min_lines: 30
  key_links:
    - from: src/app/dashboard/page.tsx
      to: /api/dashboard/vms
      via: "fetch or server component data"
      pattern: "api/dashboard/vms|fetch.*vms"
    - from: src/app/dashboard/page.tsx
      to: src/lib/provisioning/time-tracking.ts
      via: "getRemainingMinutes, isExpired"
      pattern: "getRemainingMinutes|isExpired"
---

<objective>
Build the dashboard shell with VM list, status display, and remaining time. Add polling so users see provisioning → ready without manual refresh.
</objective>

<execution_context>
@C:/Users/realg/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/realg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-user-dashboard-experience/04-CONTEXT.md
@prisma/schema.prisma
@src/lib/provisioning/time-tracking.ts
@src/app/orders/page.tsx
@src/app/layout.tsx
</context>

<interfaces>
From prisma/schema.prisma — ProvisionedVm:
  id, orderId, status, vultrInstanceId, readyAt, expiresAt, connectionMethod, connectionState, ipAddress, hostname, machineProfileId, failureCode, failureMessage

From src/lib/provisioning/time-tracking.ts:
  export function getRemainingMinutes(vm: Pick<ProvisionedVm, 'readyAt' | 'expiresAt'>): number | null;
  export function isExpired(vm: Pick<ProvisionedVm, 'expiresAt'>): boolean;

From src/app/orders/page.tsx — statusLabel pattern:
  function statusLabel(status: string): string { switch(status) { case 'pending_payment': return 'Aguardando pagamento'; ... } }

ProvisionedVm status values used in Phase 3: payment_confirmed, provisioning, vm_ready, expiring, destroying, destroyed, failed
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard API and VM list</name>
  <files>src/app/api/dashboard/vms/route.ts</files>
  <read_first>
    - src/lib/auth/session.ts
    - src/app/api/orders/route.ts
    - prisma/schema.prisma
  </read_first>
  <action>
    Create GET /api/dashboard/vms that:
    1. Uses getSessionFromCookies(); if no session, return 401
    2. Query ProvisionedVm via Order relation: prisma.provisionedVm.findMany({ where: { order: { userId: session.user.id } }, include: { order: { include: { plan: true } }, machineProfile: true }, orderBy: { createdAt: 'desc' } })
    3. Return JSON: { vms: Array of { id, status, readyAt, expiresAt, connectionMethod, connectionState, ipAddress, hostname, machineProfileId, machineProfileName, planName, orderId } } — exclude sensitive fields (windowsUsername, secretReference)
    4. Map status to user-facing labels: provisioning → "Provisionando", vm_ready → "Pronto", expiring → "Expirando", destroying → "Encerrando", destroyed → "Encerrado", failed → "Falhou"
  </action>
  <verify>
    <automated>curl -s -b "session=..." http://localhost:3000/api/dashboard/vms returns 200 with vms array when authenticated</automated>
  </verify>
  <acceptance_criteria>
    - File src/app/api/dashboard/vms/route.ts exists
    - GET returns 401 when unauthenticated
    - GET returns 200 with vms array when authenticated
    - Response includes status, readyAt, expiresAt, connectionMethod, connectionState, machineProfileName, planName
    - No windowsUsername or secretReference in response
  </acceptance_criteria>
  <done>API returns user's VMs with status and metadata; no sensitive data exposed</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard page and VmStatusCard</name>
  <files>src/app/dashboard/page.tsx, src/components/VmStatusCard.tsx</files>
  <read_first>
    - src/app/orders/page.tsx
    - src/app/layout.tsx
    - src/lib/provisioning/time-tracking.ts
  </read_first>
  <action>
    1. Create src/components/VmStatusCard.tsx:
       - Props: vm (id, status, readyAt, expiresAt, machineProfileName, planName, connectionMethod, connectionState)
       - Import getRemainingMinutes, isExpired from time-tracking
       - Render: machine profile name, plan name, status badge (provisioning=amber, vm_ready=green, expiring=orange, destroyed/failed=gray)
       - If vm_ready or expiring: show "Tempo restante: X minutos" using getRemainingMinutes; if expired show "Expirado"
       - If provisioning: show "Aguardando VM ficar pronta..."
       - Use Tailwind: rounded-lg border, bg-white, p-4, consistent with orders page
    2. Create src/app/dashboard/page.tsx:
       - Server component: getSessionFromCookies(); redirect to /login?redirect=/dashboard if no session
       - Fetch VMs from /api/dashboard/vms (or use prisma directly in server component for same data)
       - Use prisma directly: prisma.provisionedVm.findMany({ where: { order: { userId } }, include: { order: { include: { plan: true } }, machineProfile: true }, orderBy: { createdAt: 'desc' } })
       - Title: "Dashboard" (or "Minhas Máquinas")
       - If no VMs: "Você ainda não tem máquinas. Faça um pedido para começar." with link to /plans
       - If VMs: render VmStatusCard for each; show active VM first
       - Layout: mx-auto max-w-4xl, match orders page styling
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "error|Error" | head -5; exit 0</automated>
  </verify>
  <acceptance_criteria>
    - src/components/VmStatusCard.tsx exists with getRemainingMinutes, isExpired usage
    - src/app/dashboard/page.tsx exists, redirects unauthenticated to /login
    - Dashboard shows VM list with status and remaining time
    - statusLabel or equivalent maps status to pt-BR labels
  </acceptance_criteria>
  <done>Dashboard renders VM list with status and remaining time; empty state shows when no VMs</done>
</task>

<task type="auto">
  <name>Task 3: Polling for real-time status updates</name>
  <files>src/app/dashboard/page.tsx, src/components/VmStatusCard.tsx</files>
  <read_first>
    - src/app/dashboard/page.tsx
    - src/components/VmStatusCard.tsx
  </read_first>
  <action>
    Add client-side polling so VM status updates without manual refresh:
    1. Create a client component wrapper (e.g. src/components/DashboardVmList.tsx) that:
       - Accepts initial vms from server
       - Uses useState for vms; useEffect to poll GET /api/dashboard/vms every 15 seconds when any VM has status "provisioning" or "payment_confirmed"
       - Stop polling when all VMs are terminal (vm_ready, destroyed, failed) or after 20 minutes
       - Render VmStatusCard for each vm
    2. Update dashboard page: fetch initial vms server-side, pass to DashboardVmList; render DashboardVmList
    3. Polling interval: 15 seconds (configurable constant)
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "error|Error" | head -5; exit 0</automated>
  </verify>
  <acceptance_criteria>
    - DashboardVmList component polls /api/dashboard/vms when provisioning VMs exist
    - Polling stops when all VMs are terminal or max duration reached
    - User sees VM move from provisioning to vm_ready without refresh
  </acceptance_criteria>
  <done>User sees VM move from provisioning to ready without refreshing manually</done>
</task>

</tasks>

<verification>
- Run: npm run build — must pass
- Manual: Log in, visit /dashboard — see VM list or empty state
- Manual: With provisioning VM, wait — status updates without refresh
</verification>

<success_criteria>
1. User can log into /dashboard and immediately see active VM list or empty state
2. VM status (provisioning, vm_ready, expiring, destroying, destroyed, failed) displayed with pt-BR labels
3. Remaining time shown for active VMs (vm_ready, expiring)
4. Polling updates status when provisioning completes
</success_criteria>

<output>
After completion, create .planning/phases/04-user-dashboard-experience/04-01-SUMMARY.md
</output>
