---
phase: 08-environment-cost-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
  - vercel.json
autonomous: true
requirements:
  - ENV-01
  - ENV-02
  - ENV-03
  - ENV-04

must_haves:
  truths:
    - "All real keys and secrets are documented in .env.example; no hardcoded credentials"
    - "CRON jobs run teardown, provisioning, and reconciliation at configured intervals"
    - "Webhook endpoint is reachable at production URL"
    - "Basic production deployment configuration exists for Vercel"
  artifacts:
    - path: .env.example
      provides: "Documented env vars for Vultr, Mercado Pago, CRON, cost guardrails"
      contains: "VULTR_API_KEY, CRON_SECRET, VULTR_MAX_ACTIVE_VMS, PROVISIONING_ENABLED"
    - path: vercel.json
      provides: "Vercel Cron schedule for teardown, provisioning, reconciliation"
      contains: "crons"
  key_links:
    - from: vercel.json
      to: "/api/cron/process-teardown"
      via: "cron schedule"
    - from: vercel.json
      to: "/api/cron/process-provisioning"
      via: "cron schedule"
    - from: vercel.json
      to: "/api/cron/reconciliation"
      via: "cron schedule"
---

<objective>
Prepare production deployment environment: document all required env vars in .env.example and create vercel.json with Vercel Cron schedules for teardown, provisioning, and reconciliation.

Purpose: ENV-01 through ENV-04 — no hardcoded credentials, CRON configured, webhooks reachable, basic deployment ready.
Output: .env.example (extended), vercel.json (new)
</objective>

<execution_context>
@C:/Users/realg/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/realg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-environment-cost-foundation/08-CONTEXT.md

User decisions (locked):
- Host: Vercel
- Cron: Vercel Cron
- Gateway: Mercado Pago
- Webhook URL: https://your-domain/api/webhooks/payments
- Initial VM cap: 10 (VULTR_MAX_ACTIVE_VMS=10)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend .env.example with production vars</name>
  <files>.env.example</files>
  <read_first>
    - .env.example
    - src/lib/provisioning/cost-safety.ts
    - src/lib/vultr/client.ts
    - src/app/api/cron/process-teardown/route.ts
  </read_first>
  <action>
Extend .env.example with all production env vars. Add these sections with comments:

1. **Vultr** (required for Phase 10):
   - VULTR_API_KEY= — Get from Vultr dashboard
   - VULTR_MAX_ACTIVE_VMS=10 — Initial cap (override via .env)
   - VULTR_WINDOWS_GPU_OS_ID=215 — Windows GPU OS image ID
   - GRACE_PERIOD_MINUTES=5 — Grace before teardown

2. **CRON** (required for Vercel Cron):
   - CRON_SECRET= — Random string; Vercel Cron sends x-cron-secret or Authorization: Bearer

3. **Cost guardrails**:
   - PROVISIONING_ENABLED=true — Set false/0 to kill switch

4. **Mercado Pago** (Phase 9 will use):
   - MERCADOPAGO_ACCESS_TOKEN= — API access token
   - MERCADOPAGO_WEBHOOK_SECRET= — For webhook signature verification
   - Document: Webhook URL = https://your-domain/api/webhooks/payments (replace your-domain with Vercel deployment URL)

Keep existing vars (DATABASE_URL, SESSION_SECRET, RESEND_API_KEY). Use placeholder values (empty or example) for secrets.
  </action>
  <acceptance_criteria>
- grep -E "VULTR_API_KEY|CRON_SECRET|VULTR_MAX_ACTIVE_VMS|PROVISIONING_ENABLED|MERCADOPAGO" .env.example
- .env.example contains VULTR_MAX_ACTIVE_VMS=10 in comment or default
- .env.example contains webhook URL documentation
  </acceptance_criteria>
  <verify>
    <automated>grep -q VULTR_API_KEY .env.example && grep -q CRON_SECRET .env.example && grep -q MERCADOPAGO .env.example</automated>
  </verify>
  <done>.env.example documents all production env vars; no secrets hardcoded in source</done>
</task>

<task type="auto">
  <name>Task 2: Create vercel.json with Vercel Cron</name>
  <files>vercel.json</files>
  <read_first>
    - src/app/api/cron/process-teardown/route.ts
    - src/app/api/cron/process-provisioning/route.ts
    - src/app/api/cron/reconciliation/route.ts
  </read_first>
  <action>
Create vercel.json at project root with Vercel Cron configuration.

Structure:
```json
{
  "crons": [
    {
      "path": "/api/cron/process-teardown",
      "schedule": "*/5 * * * *"
    },
    {
      "path": "/api/cron/process-provisioning",
      "schedule": "* * * * *"
    },
    {
      "path": "/api/cron/reconciliation",
      "schedule": "*/15 * * * *"
    }
  ]
}
```

- process-teardown: every 5 minutes
- process-provisioning: every 1 minute
- reconciliation: every 15 minutes

Vercel Cron automatically sends CRON_SECRET in the Authorization header when configured. Ensure .env has CRON_SECRET set in Vercel project settings.
  </action>
  <acceptance_criteria>
- vercel.json exists at project root
- vercel.json has "crons" array with exactly 3 entries
- grep '"path": "/api/cron/process-teardown"' vercel.json
- grep '"path": "/api/cron/process-provisioning"' vercel.json
- grep '"path": "/api/cron/reconciliation"' vercel.json
  </acceptance_criteria>
  <verify>
    <automated>node -e "const v=require('./vercel.json'); if(!v.crons||v.crons.length!==3)process.exit(1); const paths=v.crons.map(c=>c.path); if(!paths.includes('/api/cron/process-teardown')||!paths.includes('/api/cron/process-provisioning')||!paths.includes('/api/cron/reconciliation'))process.exit(1);"</automated>
  </verify>
  <done>vercel.json created; CRON routes scheduled for teardown (5min), provisioning (1min), reconciliation (15min)</done>
</task>

</tasks>

<verification>
- .env.example contains all required vars; grep confirms VULTR, CRON, PROVISIONING, MERCADOPAGO
- vercel.json crons array has 3 entries; node validation passes
- npm run build succeeds (vercel.json is valid JSON)
</verification>

<success_criteria>
1. .env.example documents Vultr, CRON, cost guardrails, Mercado Pago vars
2. vercel.json exists with 3 cron entries for teardown, provisioning, reconciliation
3. No hardcoded credentials in source; deployment config ready for Vercel
</success_criteria>

<output>
After completion, create .planning/phases/08-environment-cost-foundation/08-01-SUMMARY.md
</output>
