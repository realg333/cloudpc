---
phase: 07-dashboard-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/dashboard/page.tsx
  - src/app/globals.css
autonomous: true
requirements:
  - DASH-01
  - DASH-05
must_haves:
  truths:
    - "User sees dashboard in dark mode aligned with landing/plans"
    - "User sees clear visual hierarchy and spacing on dashboard"
    - "Empty state guides user persuasively to /plans with compelling CTA"
  artifacts:
    - path: src/app/dashboard/page.tsx
      provides: Dashboard shell with dark theme and empty state
      contains: "dark-plans"
    - path: src/app/globals.css
      provides: Body background for dark-plans routes
      contains: "body:has(.dark-plans)"
  key_links:
    - from: src/app/dashboard/page.tsx
      to: /plans
      via: Link href
      pattern: "href=\"/plans\""
---

<objective>
Apply dark mode to the dashboard page and convert the empty state to a persuasive, premium guide to /plans. Align with landing page and plans page dark layout using .dark-plans tokens. Per CONTEXT: dark mode, subtle indigo/violet accents, clean sharp modern, premium feel; empty state must be persuasive and compelling.
</objective>

<execution_context>
@C:/Users/realg/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/realg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-dashboard-polish/07-CONTEXT.md
@src/app/dashboard/page.tsx
@src/app/globals.css
@src/app/plans/page.tsx
</context>

<interfaces>
From src/app/globals.css — .dark-plans tokens:
  --dm-bg: #0a0a0f; --dm-bg-elevated: #12121a; --dm-bg-card: #16161f; --dm-border: rgba(255,255,255,0.08);
  --dm-text: #f1f5f9; --dm-text-muted: #94a3b8; --dm-text-subtle: #64748b; --dm-accent: #6366f1;
  --dm-cta: #f97316; --dm-cta-hover: #ea580c; --dm-success: #22c55e;

From src/app/plans/page.tsx — dark wrapper pattern:
  className="dark-plans relative -mt-8 left-1/2 right-1/2 -ml-[50vw] -mr-[50vw] w-screen min-h-screen overflow-x-hidden bg-[#0a0a0f] text-slate-100"
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add dark-plans wrapper and body background</name>
  <files>src/app/dashboard/page.tsx, src/app/globals.css</files>
  <read_first>
    - src/app/dashboard/page.tsx
    - src/app/plans/page.tsx
    - src/app/globals.css
  </read_first>
  <action>
    1. In src/app/globals.css: Add body:has(.dark-plans) { background: #0a0a0f; } after the existing body:has(.dark-layout) block (line ~68). This ensures body background is dark when dashboard or plans is shown.
    2. In src/app/dashboard/page.tsx: Wrap the entire return content in a dark-plans div. Use the exact pattern from plans page: className="dark-plans relative -mt-8 left-1/2 right-1/2 -ml-[50vw] -mr-[50vw] w-screen min-h-screen overflow-x-hidden bg-[#0a0a0f] text-slate-100"
    3. Keep the inner structure: mx-auto max-w-4xl for the content container.
    4. Update the header (h1, p, Link): Replace text-slate-900 with text-white; text-slate-600 with text-slate-400; primary hover with text-indigo-400 hover:text-indigo-300; add focus:ring-offset-[#0a0a0f] for focus rings so they are visible on dark bg.
    5. Header h1: text-2xl font-bold tracking-tight text-white sm:text-3xl
    6. Header p: mt-1.5 text-base text-slate-400
    7. Header Link: text-indigo-400 hover:text-indigo-300 transition-colors focus:ring-offset-[#0a0a0f]
  </action>
  <verify>
    <automated>rg "dark-plans" src/app/dashboard/page.tsx && rg "body:has\(\.dark-plans\)" src/app/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - src/app/globals.css contains body:has(.dark-plans) { background: #0a0a0f; }
    - src/app/dashboard/page.tsx root wrapper has class dark-plans and bg-[#0a0a0f]
    - Header h1 uses text-white
    - Header p uses text-slate-400
  </acceptance_criteria>
  <done>Dashboard page uses dark-plans wrapper; body background dark when dashboard shown; header uses dark theme colors.</done>
</task>

<task type="auto">
  <name>Task 2: Convert empty state to dark, persuasive guide to /plans</name>
  <files>src/app/dashboard/page.tsx</files>
  <read_first>
    - src/app/dashboard/page.tsx
    - .planning/phases/07-dashboard-polish/07-CONTEXT.md
  </read_first>
  <action>
    1. Replace the empty state div styling. Current: rounded-2xl border border-slate-200/80 bg-gradient-to-br from-white to-slate-50/50. New: rounded-2xl border border-white/10 bg-[#16161f] (dm-bg-card equivalent) with subtle indigo glow. Add: shadow-[0_0_40px_rgba(99,102,241,0.08)] for premium feel.
    2. Icon container: Replace bg-primary/10 text-primary with bg-indigo-500/20 text-indigo-400. Use same SVG, ensure aria-hidden on decorative elements.
    3. Heading h2: Replace text-slate-900 with text-white. Keep "Sua primeira máquina está a um clique".
    4. Paragraph: Replace text-slate-600 with text-slate-400. Keep value proposition text.
    5. Bullet list (GPU dedicada, etc.): Replace text-slate-600 with text-slate-400; replace bg-emerald-500 dots with bg-indigo-400 or bg-emerald-500 (keep green for positive connotation).
    6. Primary CTA (Ver planos e começar): Use gradient matching landing: bg-gradient-to-r from-indigo-500 to-violet-600, shadow-[0_4px_20px_rgba(99,102,241,0.4)], hover:from-indigo-400 hover:to-violet-500, min-h-[52px], focus:ring-offset-[#0a0a0f]. Keep min-w-[200px], rounded-xl.
    7. Secondary CTA (Ver histórico): border border-white/20 bg-white/5 text-slate-300 hover:bg-white/10 hover:text-white, focus:ring-offset-[#0a0a0f].
    8. Ensure touch targets: min-h-[52px] on primary CTA; secondary CTA min-h-[44px] per UI UX Pro Max.
    9. Preserve role="status" and aria-label="Nenhuma máquina provisionada" on the empty state container.
  </action>
  <verify>
    <automated>rg "Ver planos e começar|Sua primeira máquina" src/app/dashboard/page.tsx && rg "bg-\[#16161f\]|border-white/10" src/app/dashboard/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Empty state container has bg-[#16161f] or equivalent dark card background
    - Primary CTA has from-indigo-500 to-violet-600 gradient
    - Primary CTA links to href="/plans"
    - Heading uses text-white
    - Body text uses text-slate-400
    - aria-label="Nenhuma máquina provisionada" preserved
  </acceptance_criteria>
  <done>Empty state is persuasive, dark-themed, with strong CTA to /plans; premium feel; accessible.</done>
</task>

</tasks>

<verification>
- Visit /dashboard when logged in with no VMs: see dark empty state, persuasive copy, gradient CTA to /plans
- Visit /dashboard when logged in with VMs: see dark header, content area
- Body background is dark (#0a0a0f) when on dashboard
</verification>

<success_criteria>
- Dashboard page uses dark-plans theme throughout
- Empty state guides user to /plans with compelling CTA
- Visual hierarchy clear; spacing consistent
</success_criteria>

<output>
After completion, create .planning/phases/07-dashboard-polish/07-dashboard-polish-01-SUMMARY.md
</output>
