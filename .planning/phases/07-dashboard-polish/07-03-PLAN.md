---
phase: 07-dashboard-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/DashboardVmList.tsx
autonomous: true
requirements:
  - DASH-01
  - DASH-05
must_haves:
  truths:
    - "Active/featured machine dominates the page visually"
    - "Dashboard meets UI UX Pro Max: touch targets ≥44px, focus rings visible, responsive"
  artifacts:
    - path: src/components/DashboardVmList.tsx
      provides: VM list with featured hero, dark styling, UX compliance
      contains: "active-vm-heading"
  key_links:
    - from: src/components/DashboardVmList.tsx
      to: VmStatusCard
      via: import and render
      pattern: "VmStatusCard"
---

<objective>
Apply dark theme to DashboardVmList, ensure featured VM dominates visually, and align with UI UX Pro Max guidelines (accessibility, touch targets, focus states, responsive layout). Per CONTEXT: active machine must dominate; clean hierarchy; no clutter.
</objective>

<execution_context>
@C:/Users/realg/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/realg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-dashboard-polish/07-CONTEXT.md
@src/components/DashboardVmList.tsx
@skills/ui-ux-pro-max/SKILL.md
</context>

<interfaces>
From src/components/DashboardVmList.tsx:
  - featuredVm: first active (vm_ready/expiring) or first provisioning
  - otherVms: rest of sorted list
  - VmStatusCard receives featured={true} for featured VM

From UI UX Pro Max — Critical checks:
  - touch-target-size: min 44×44pt
  - focus-states: visible focus rings 2–4px
  - color-contrast: 4.5:1 for text
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Dark theme and featured VM dominance</name>
  <files>src/components/DashboardVmList.tsx</files>
  <read_first>
    - src/components/DashboardVmList.tsx
    - .planning/phases/07-dashboard-polish/07-CONTEXT.md
  </read_first>
  <action>
    1. Featured section wrapper: Wrap the featured VmStatusCard in a div with className="rounded-2xl bg-[#12121a] p-1 ring-1 ring-white/5". This creates an elevated surface (dm-bg-elevated) so the active machine dominates visually.
    2. "Outras máquinas" section: Update h2 from text-slate-500 to text-slate-400. Add id="other-vms-heading" to the h2.
    3. Section spacing: Change space-y-8 to space-y-10 for clearer hierarchy when both featured and others exist.
    4. Do NOT change the sort order or featured logic — keep as is.
  </action>
  <verify>
    <automated>rg "active-vm-heading|Outras máquinas|featured" src/components/DashboardVmList.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Featured VM section has elevated styling (bg-[#12121a] or ring/glow)
    - "Outras máquinas" h2 uses text-slate-400
    - aria-labelledby="active-vm-heading" preserved
    - space-y-10 or equivalent for section spacing
  </acceptance_criteria>
  <done>Featured VM dominates visually; "Outras máquinas" section has dark theme; clear hierarchy.</done>
</task>

<task type="auto">
  <name>Task 2: UI UX Pro Max alignment — accessibility, touch, focus</name>
  <files>src/components/DashboardVmList.tsx</files>
  <read_first>
    - src/components/DashboardVmList.tsx
    - skills/ui-ux-pro-max/SKILL.md
  </read_first>
  <action>
    1. Verify VmStatusCard is passed correct props — we don't modify VmStatusCard here; Plan 02 handles that. DashboardVmList only renders VmStatusCard. Ensure any links or buttons in DashboardVmList (if added) have focus rings.
    2. Section headings: Ensure h2 "Outras máquinas" has id for aria-labelledby if the list needs it. Add id="other-vms-heading" to the h2, and aria-labelledby="other-vms-heading" on the ul for "Outras máquinas".
    3. List semantics: ul has role="list" and aria-label="Outras máquinas virtuais" — keep these. Ensure each li has no redundant roles.
    4. Spacing between list items: space-y-4 gives 16px gap — meets 8px minimum between touch targets per UI UX Pro Max. Keep or increase to space-y-5 (20px) for comfort.
    5. Responsive: Ensure the layout works on mobile (stacked) and desktop. Current structure is already responsive (no fixed widths on cards). Verify max-w-4xl from parent doesn't cause horizontal scroll on 375px viewport.
    6. Focus management: VmStatusCard contains the Connect button; Plan 02 adds focus:ring-offset-[#0a0a0f]. No additional focus work needed in DashboardVmList unless we add interactive elements.
    7. Reduced motion: VmStatusCard may have animations; globals.css already has prefers-reduced-motion. No changes needed in DashboardVmList for reduced motion.
    8. Add skip link consideration: Dashboard page has main content; ensure the featured section is first in tab order (it is, by DOM order). No change needed.
  </action>
  <verify>
    <automated>rg "aria-label|aria-labelledby|id=" src/components/DashboardVmList.tsx</automated>
  </verify>
  <acceptance_criteria>
    - "Outras máquinas" ul has aria-labelledby pointing to heading id
    - h2 has id="other-vms-heading"
    - space-y-4 or space-y-5 between list items
  </acceptance_criteria>
  <done>DashboardVmList meets UI UX Pro Max: semantic structure, aria attributes, spacing.</done>
</task>

</tasks>

<verification>
- With multiple VMs: featured one dominates; others in compact list
- Tab order: featured card first, then others
- Screen reader: headings and list properly labelled
</verification>

<success_criteria>
- Featured VM is focal point
- Dark theme applied to list sections
- Accessibility and UX guidelines met
</success_criteria>

<output>
After completion, create .planning/phases/07-dashboard-polish/07-dashboard-polish-03-SUMMARY.md
</output>
