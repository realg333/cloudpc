---
phase: 07-dashboard-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/VmStatusCard.tsx
autonomous: true
requirements:
  - DASH-02
  - DASH-03
  - DASH-04
must_haves:
  truths:
    - "User can instantly distinguish machine status (provisioning, ready, expiring, destroyed)"
    - "User sees remaining time prominently with countdown and/or progress"
    - "Connect CTA is the strongest, most visible element when VM is connectable"
  artifacts:
    - path: src/components/VmStatusCard.tsx
      provides: VM status card with dark theme, status clarity, time display, Connect CTA
      contains: "statusConfig"
  key_links:
    - from: src/components/VmStatusCard.tsx
      to: getRemainingMinutes, isExpired
      via: import from time-tracking
      pattern: "getRemainingMinutes|isExpired"
---

<objective>
Apply dark theme to VmStatusCard, make each status (provisioning, ready, expiring, destroyed) visually distinct, make remaining time highly visible and dynamic, and make the Connect CTA the strongest element on the page. Per CONTEXT: status clarity with color/layout; remaining time prominent (countdown, progress, or both); Connect button impossible to miss.
</objective>

<execution_context>
@C:/Users/realg/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/realg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-dashboard-polish/07-CONTEXT.md
@src/components/VmStatusCard.tsx
@src/app/globals.css
@src/lib/provisioning/time-tracking.ts
</context>

<interfaces>
From src/app/globals.css — .dark-plans tokens:
  --dm-bg-card: #16161f; --dm-border: rgba(255,255,255,0.08); --dm-accent: #6366f1;
  --dm-cta: #f97316; --dm-cta-hover: #ea580c; --dm-success: #22c55e;

From src/lib/provisioning/time-tracking.ts:
  export function getRemainingMinutes(vm): number | null;
  export function isExpired(vm): boolean;

VmStatusCard status flow: payment_confirmed, provisioning → vm_ready → expiring → destroying → destroyed, failed
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Apply dark theme statusConfig — each status visually distinct</name>
  <files>src/components/VmStatusCard.tsx</files>
  <read_first>
    - src/components/VmStatusCard.tsx
    - src/app/globals.css
  </read_first>
  <action>
    1. Replace statusConfig in VmStatusCard with dark-theme variants. Card backgrounds use bg-[#16161f], border-white/10. Each status must be clearly different:
    2. provisioning/payment_confirmed: card border-amber-500/40, iconBg bg-amber-500/20, iconColor text-amber-400, badge bg-amber-500/20 text-amber-300 border-amber-500/30. Accent amber.
    3. vm_ready + canConnect: card border-emerald-500/40, iconBg bg-emerald-500/20, iconColor text-emerald-400, badge bg-emerald-500/20 text-emerald-300 border-emerald-500/30. Accent emerald.
    4. expiring: card border-orange-500/40, iconBg bg-orange-500/20, iconColor text-orange-400, badge bg-orange-500/20 text-orange-300 border-orange-500/30. Accent orange.
    5. destroying: same as expiring (orange).
    6. destroyed/failed: card border-white/5, iconBg bg-slate-500/20, iconColor text-slate-400, badge bg-slate-500/20 text-slate-400 border-slate-500/20. Accent slate.
    7. Default/other: card border-white/10, iconBg bg-slate-500/20, iconColor text-slate-400.
    8. Base card: always use bg-[#16161f] (dm-bg-card), border, rounded-2xl. Replace all light-theme card classes (border-amber-200, bg-gradient-to-br from-amber-50, etc.) with the dark equivalents above.
    9. Header text: h3 use text-white instead of text-slate-900; plan name p use text-slate-400 instead of text-slate-600.
    10. Provisioning shimmer: use bg-amber-500/30 for the animated bar; label text-amber-300; helper text text-slate-400.
    11. Terminal state text: text-slate-400.
  </action>
  <verify>
    <automated>rg "bg-\[#16161f\]|border-amber-500/40|border-emerald-500/40|border-orange-500/40" src/components/VmStatusCard.tsx</automated>
  </verify>
  <acceptance_criteria>
    - statusConfig has distinct card/badge/icon for provisioning, vm_ready, expiring, destroyed
    - Card uses bg-[#16161f] for dark background
    - Header h3 uses text-white
    - Plan name uses text-slate-400
  </acceptance_criteria>
  <done>Each status (provisioning, ready, expiring, destroyed) has distinct color and visual treatment; card uses dark theme.</done>
</task>

<task type="auto">
  <name>Task 2: Make remaining time prominent — countdown + progress bar</name>
  <files>src/components/VmStatusCard.tsx</files>
  <read_first>
    - src/components/VmStatusCard.tsx
    - src/lib/provisioning/time-tracking.ts
  </read_first>
  <action>
    1. Add client-side countdown: use useState + useEffect to update remaining minutes every 60 seconds (or every 1 second when remaining < 5 minutes). Call getRemainingMinutes with current vm.readyAt, vm.expiresAt. Store in state; update on interval.
    2. Time display section: Make it visually dominant. When featured: use text-2xl or text-xl font-bold tabular-nums for the remaining time value. When not featured: use text-lg font-semibold.
    3. Label: "Tempo restante" (not "Tempo de sessão") — more direct. Use text-slate-400 for label.
    4. Progress bar: Increase height from h-2.5 to h-3 when featured, keep h-2.5 when not. Use bg-white/10 for track; fill color: emerald-500 when &lt;70%, orange-500 when 70-90%, red-500 when ≥90% or expired.
    5. Add urgency styling when remaining &lt; 30 min: subtle pulse or stronger color (e.g. text-orange-400 when 30-60 min, text-red-400 when &lt;30 min). Use tabular-nums for all time values to prevent layout shift.
    6. Format: Keep formatRemaining logic (Xh Ymin, or X min when &lt;60). Ensure countdown updates in real time for last minutes.
    7. Respect prefers-reduced-motion: no pulse animation when reduced-motion; use static color only.
  </action>
  <verify>
    <automated>rg "Tempo restante|getRemainingMinutes|tabular-nums" src/components/VmStatusCard.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Time section shows "Tempo restante" label
    - Remaining value uses tabular-nums
    - Progress bar present when readyAt and expiresAt exist
    - featured card has larger time text (text-xl or text-2xl)
  </acceptance_criteria>
  <done>Remaining time is prominent, dynamic (updates), and visually strong; progress bar reflects usage.</done>
</task>

<task type="auto">
  <name>Task 3: Make Connect CTA the strongest element</name>
  <files>src/components/VmStatusCard.tsx</files>
  <read_first>
    - src/components/VmStatusCard.tsx
    - src/app/globals.css
  </read_first>
  <action>
    1. When featured AND canConnect: Connect button must dominate. Use: min-h-[60px] (not 56px), px-10 py-4, text-xl font-bold. Background: bg-gradient-to-r from-[#f97316] to-[#ea580c] (dm-cta gradient). Shadow: shadow-[0_4px_24px_rgba(249,115,22,0.45)]. Hover: brighter gradient, shadow-[0_6px_32px_rgba(249,115,22,0.5)]. Active: scale-[0.98].
    2. Focus: focus:ring-2 focus:ring-orange-400 focus:ring-offset-2 focus:ring-offset-[#0a0a0f] — visible on dark.
    3. When not featured but canConnect: min-h-[52px], px-8, text-lg font-semibold, same orange gradient, smaller shadow.
    4. Button text: "Conectar agora" with Zap icon. Ensure aria-label="Conectar via Parsec" for accessibility.
    5. Loading state: disabled, show spinner, "Conectando..." — keep existing logic.
    6. Copy feedback (copied/error): use text-emerald-400 and text-red-400 for dark theme.
    7. "Baixar Parsec" link: text-indigo-400 hover:text-indigo-300.
    8. Touch target: featured button min-h-[60px] meets 44pt minimum; ensure padding creates ≥44px tap area.
  </action>
  <verify>
    <automated>rg "Conectar agora|from-\[#f97316\]|min-h-\[60px\]|aria-label.*Conectar" src/components/VmStatusCard.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Featured Connect button has min-h-[60px], orange gradient, prominent shadow
    - Button has aria-label for screen readers
    - "Conectar agora" text with Zap icon
    - focus:ring-offset-[#0a0a0f] for visible focus ring
  </acceptance_criteria>
  <done>Connect CTA is the strongest, most visible element when VM is connectable; impossible to miss.</done>
</task>

</tasks>

<verification>
- With vm_ready + connectionState ready: Connect button dominates; remaining time prominent
- With expiring: orange status, time visible
- With provisioning: amber status, no Connect
- With destroyed: slate/muted
</verification>

<success_criteria>
- Status clarity: provisioning/ready/expiring/destroyed each visually distinct
- Remaining time: prominent, dynamic, progress bar
- Connect CTA: strongest element when connectable
</success_criteria>

<output>
After completion, create .planning/phases/07-dashboard-polish/07-dashboard-polish-02-SUMMARY.md
</output>
