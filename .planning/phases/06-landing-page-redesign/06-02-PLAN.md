---
phase: 06-landing-page-redesign
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/app/page.tsx
  - src/app/globals.css
autonomous: true
requirements:
  - LAND-04
must_haves:
  truths:
    - "User experiences cohesive visual design with clear hierarchy"
    - "All interactive elements meet accessibility standards (contrast, focus, touch targets)"
  artifacts:
    - path: src/app/page.tsx
      provides: Landing page with UI UX Pro Max alignment
    - path: src/app/globals.css
      provides: Design tokens (unchanged or refined)
  key_links:
    - from: All Link/button elements
      to: focus ring, min 44px touch target
      via: Tailwind classes
      pattern: "min-h-\[44px\]|min-h-\[52px\]|focus:ring"
---

<objective>
Apply UI UX Pro Max guidelines to the landing page: accessibility (contrast 4.5:1, focus states, aria-labels), touch targets (min 44px), visual hierarchy, and cohesive design. Refine within existing dark premium style (indigo/violet, no childish/RGB gamer look).
</objective>

<execution_context>
@C:/Users/realg/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/realg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-landing-page-redesign/06-CONTEXT.md
@src/app/page.tsx
@src/app/globals.css
@skills/ui-ux-pro-max/SKILL.md
</context>

<interfaces>
From UI UX Pro Max SKILL.md — Critical rules:
- color-contrast: 4.5:1 for normal text, 3:1 for large text
- focus-states: Visible focus rings (2–4px) on interactive elements
- touch-target-size: Min 44×44pt (Apple) / 48×48dp (Material)
- touch-spacing: Min 8px gap between touch targets
- skip-links: Skip to main content (already present)
- heading-hierarchy: Sequential h1→h6, no level skip
- aria-labels: For icon-only buttons
- primary-action: One primary CTA per screen; secondary visually subordinate

From globals.css — Existing tokens: --color-primary, --glass-bg, .dark-plans, prefers-reduced-motion
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Accessibility and touch target audit</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - src/app/page.tsx
    - skills/ui-ux-pro-max/SKILL.md
  </read_first>
  <action>
    1. Audit all interactive elements: Links, buttons, trust strip badges (if tappable). Ensure each has min-height 44px and min-width 44px (or equivalent padding for touch area). CTAs already use min-h-[52px] — verify all CTAs and secondary links meet 44px minimum.
    2. Add aria-label to icon-only elements: ChevronRight in CTAs has aria-hidden (correct for decorative). Ensure no icon-only buttons without labels. Primary/secondary CTAs have visible text — no change needed.
    3. Verify focus rings: All Link/button elements must have focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2 focus:ring-offset-[background]. Check primary CTA, secondary CTA, plans CTA, final CTA, and any trust strip badges that are links.
    4. Verify heading hierarchy: h1 in hero, h2 for section headings (value-heading, use-cases-heading, how-it-works-heading, performance-heading, plans-heading). No skipped levels.
    5. Verify contrast: Body text (text-slate-400) on bg-[#050506] and bg-[#0a0a0c]. Slate-400 on #0a0a0c ≈ 5:1. If any text fails 4.5:1, upgrade to text-slate-300. Headings (text-white) on dark bg meet contrast.
    6. Ensure skip link remains: "Pular para o conteúdo principal" with focus:block behavior.
  </action>
  <verify>
    <automated>rg "focus:ring" src/app/page.tsx | head -5 && rg "min-h-\[4[48]px\]|min-h-\[52px\]" src/app/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Every Link and button has focus:ring-2 or equivalent focus ring
    - Every CTA/link has min-h at least 44px (44, 48, or 52)
    - No icon-only interactive element without aria-label
    - Heading structure: one h1, multiple h2, no h3 skip from h1
  </acceptance_criteria>
  <done>All interactive elements have focus rings and meet touch target minimums; heading hierarchy and contrast verified.</done>
</task>

<task type="auto">
  <name>Task 2: Visual hierarchy and spacing polish</name>
  <files>src/app/page.tsx, src/app/globals.css</files>
  <read_first>
    - src/app/page.tsx
    - src/app/globals.css
    - skills/ui-ux-pro-max/SKILL.md
  </read_first>
  <action>
    1. Apply 8px spacing rhythm: Ensure section padding (py-20, py-24) and internal gaps (gap-4, gap-6, gap-8) use multiples of 4 (4, 8, 12, 16, 20, 24). Current values already follow this — verify consistency.
    2. Section spacing hierarchy: Hero py, trust strip compact (py-8 or py-10), content sections py-20, final CTA py-24. Ensure clear visual separation between sections via border-t border-white/5 and alternating bg-[#050506]/bg-[#0a0a0c].
    3. Typography: Body text min 16px (text-base or text-lg). Current text-lg and text-sm — ensure no body copy below 14px. Labels/captions may use text-sm.
    4. Trust strip badges: If they are interactive (e.g. for future expansion), ensure min 44px touch target. If non-interactive, no change. Add 8px gap between badges (gap-2 = 8px).
    5. Preserve dark premium style: Keep bg-[#050506], bg-[#0a0a0c], indigo/violet gradients, no RGB gamer aesthetic. Do not introduce new colors that clash.
    6. Reduced motion: globals.css already has prefers-reduced-motion media query. Ensure no new animations override it.
  </action>
  <verify>
    <automated>rg "py-20|py-24|py-8|py-10" src/app/page.tsx && rg "prefers-reduced-motion" src/app/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - Section padding uses py-8, py-20, or py-24
    - Trust strip has appropriate vertical padding (py-8 or py-10)
    - globals.css contains prefers-reduced-motion block
  </acceptance_criteria>
  <done>Spacing rhythm and visual hierarchy aligned with UI UX Pro Max; dark premium style preserved.</done>
</task>

</tasks>

<verification>
- Run Lighthouse accessibility audit on / (or manual check: tab through page, verify focus visible)
- Verify touch targets on mobile viewport (375px): all CTAs and links tappable
- Confirm no contrast failures (e.g. gray-on-gray)
</verification>

<success_criteria>
- All interactive elements have visible focus rings and min 44px touch targets
- Heading hierarchy correct (h1, h2)
- Contrast meets 4.5:1 for body text
- Spacing follows 8px rhythm
- Dark premium style preserved
</success_criteria>

<output>
After completion, create .planning/phases/06-landing-page-redesign/06-landing-page-redesign-02-SUMMARY.md
</output>
