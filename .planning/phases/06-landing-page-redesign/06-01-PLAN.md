---
phase: 06-landing-page-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.tsx
autonomous: true
requirements:
  - LAND-01
  - LAND-02
  - LAND-03
must_haves:
  truths:
    - "User sees trust strip immediately after hero (reduces hesitation)"
    - "User sees primary CTA 'Ver planos' and secondary CTA 'Como funciona' in hero"
    - "Section order matches conversion flow: Hero → Trust → Value → Use cases → How it works → Performance → Plans → Final CTA"
  artifacts:
    - path: src/app/page.tsx
      provides: Landing page with reordered sections and trust strip
      contains: "id=\"trust-strip\""
  key_links:
    - from: Hero secondary CTA
      to: "#how-it-works-heading"
      via: href anchor
      pattern: "href=\"#how-it-works-heading\""
---

<objective>
Restructure the landing page for conversion: reorder sections, add compact trust strip immediately after hero, and add secondary CTA "Como funciona" in hero. Per CONTEXT.md: trust strip reduces hesitation; section order optimizes flow toward /plans.
</objective>

<execution_context>
@C:/Users/realg/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/realg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-landing-page-redesign/06-CONTEXT.md
@src/app/page.tsx
@src/app/globals.css
</context>

<interfaces>
From 06-CONTEXT.md — Section order (locked):
1. Hero
2. Trust / credibility strip (right after hero)
3. Value explanation
4. Use cases
5. How it works
6. Performance / power
7. Plans preview
8. Final CTA

Trust elements (locked): Infraestrutura no Brasil, Provisionamento rápido, PIX e cripto, Sem fidelidade, Ativação em minutos, Suporte a jogos, edição e programação

Hero CTAs (locked): Primary "Ver planos", Secondary "Como funciona" (anchor to How it works)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Reorder sections and add trust strip after hero</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - src/app/page.tsx
    - .planning/phases/06-landing-page-redesign/06-CONTEXT.md
  </read_first>
  <action>
    1. Reorder sections in page.tsx to match CONTEXT: Hero → Trust strip → Value → Use cases → How it works → Performance → Plans preview → Final CTA.
    2. Add a compact trust strip section (id="trust-strip") immediately after the hero, before Value. Use a horizontal row of 6 badge-like items: Infraestrutura no Brasil, Provisionamento rápido, PIX e cripto, Sem fidelidade, Ativação em minutos, Suporte a jogos, edição e programação.
    3. Style the trust strip: compact, single row on desktop; wrap to 2 rows on mobile. Use existing design tokens (border-white/10, bg-white/5, text-slate-400). Icons optional; text-only badges acceptable. Min touch target 44px per UI UX Pro Max.
    4. Remove the existing "Por que confiar" TrustCard section — trust strip replaces it to keep page focused per CONTEXT ("Avoid unnecessary sections").
    5. Do NOT add fake testimonials. Trust strip uses credibility badges only.
  </action>
  <verify>
    <automated>rg "id=\"trust-strip\"" src/app/page.tsx && rg "Infraestrutura no Brasil|Provisionamento rápido|PIX e cripto|Sem fidelidade|Ativação em minutos|Suporte a jogos" src/app/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File contains id="trust-strip" on a section element
    - File contains all 6 trust element strings (Infraestrutura no Brasil, Provisionamento rápido, PIX e cripto, Sem fidelidade, Ativação em minutos, Suporte a jogos)
    - Section order in JSX: Hero, trust-strip, value (O que é um Cloud PC?), use cases, how it works, performance, plans preview, final CTA
  </acceptance_criteria>
  <done>Trust strip appears immediately after hero; section order matches CONTEXT.md.</done>
</task>

<task type="auto">
  <name>Task 2: Add secondary CTA "Como funciona" in hero</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - src/app/page.tsx
    - .planning/phases/06-landing-page-redesign/06-CONTEXT.md
  </read_first>
  <action>
    1. Add secondary CTA link in hero (inside the flex container with primary CTA). Text: "Como funciona". href: "#how-it-works-heading".
    2. Style as secondary: border border-white/20, bg-white/5, text-slate-300, hover:bg-white/10 hover:text-white. Min height 52px, min width 160px. Same focus ring pattern as primary (focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2 focus:ring-offset-[#050506]).
    3. Keep primary CTA "Ver planos" (href="/plans") as the first/left button. Secondary "Como funciona" as second/right.
    4. On mobile (flex-col), primary first, secondary second.
    5. Ensure only two CTAs in hero per user decision — no additional actions.
  </action>
  <verify>
    <automated>rg "Como funciona" src/app/page.tsx && rg "href=\"#how-it-works-heading\"" src/app/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Hero contains Link or anchor with text "Como funciona" and href="#how-it-works-heading"
    - Hero CTA container has exactly two CTA elements: primary "Ver planos", secondary "Como funciona"
  </acceptance_criteria>
  <done>Secondary CTA "Como funciona" anchors to How it works section; hero has exactly two CTAs.</done>
</task>

</tasks>

<verification>
- Visit / and confirm trust strip appears below hero
- Confirm section order: Hero → Trust strip → Value → Use cases → How it works → Performance → Trust → Plans → Final CTA
- Click "Como funciona" and confirm scroll to How it works section
</verification>

<success_criteria>
- Trust strip with 6 elements appears immediately after hero
- Hero has primary "Ver planos" and secondary "Como funciona" CTAs
- Section order matches CONTEXT.md
- No fake testimonials
</success_criteria>

<output>
After completion, create .planning/phases/06-landing-page-redesign/06-landing-page-redesign-01-SUMMARY.md
</output>
