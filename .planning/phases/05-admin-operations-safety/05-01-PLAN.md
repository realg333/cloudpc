---
phase: 05-admin-operations-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations
  - next.config.mjs
  - src/lib/auth/admin.ts
  - src/app/admin/page.tsx
  - src/app/admin/layout.tsx
  - src/components/NavBar.tsx
  - prisma/seed.ts
autonomous: true
requirements:
  - ADMIN-01
  - SAFE-04
must_haves:
  truths:
    - Admin can access /admin when logged in and isAdmin=true
    - Non-admin users receive 403 when accessing /admin
    - Unauthenticated users redirect to /login when accessing /admin
  artifacts:
    - path: src/lib/auth/admin.ts
      provides: requireAdmin helper
      exports: ["requireAdmin"]
    - path: src/app/admin/page.tsx
      provides: Admin panel shell
    - path: prisma/schema.prisma
      provides: User.isAdmin field
  key_links:
    - from: src/app/admin/page.tsx
      to: src/lib/auth/admin.ts
      via: requireAdmin call
      pattern: "requireAdmin"
---

<objective>
Establish admin authentication and admin panel shell. Add isAdmin to User model, requireAdmin helper, and /admin route that redirects unauthenticated users to login and returns 403 for non-admin users.
</objective>

<execution_context>
@C:/Users/realg/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/realg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-admin-operations-safety/05-CONTEXT.md
@src/lib/auth/session.ts
@src/app/dashboard/page.tsx
</context>

<interfaces>
From src/lib/auth/session.ts:
```typescript
export async function getSessionFromCookies(): Promise<{ user: User; session: Session } | null>;
```

From prisma/schema.prisma User model:
```prisma
model User {
  id             String    @id @default(cuid())
  email          String    @unique
  passwordHash   String
  ...
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add isAdmin to User and requireAdmin helper</name>
  <files>prisma/schema.prisma, prisma/migrations, src/lib/auth/admin.ts</files>
  <read_first>
    - prisma/schema.prisma
    - src/lib/auth/session.ts
  </read_first>
  <action>
    1. Add `isAdmin Boolean @default(false)` to User model in prisma/schema.prisma.
    2. Run `npx prisma migrate dev --name add_user_is_admin` to create migration.
    3. Enable forbidden() in next.config.mjs: add `experimental: { authInterrupts: true }` to nextConfig.
    4. Create src/lib/auth/admin.ts with:
       - `requireAdmin(): Promise&lt;{ user: User; session: Session }&gt;` — calls getSessionFromCookies(); if null, call redirect('/login?redirect=/admin'); if !user.isAdmin, call forbidden() from next/navigation (returns HTTP 403). Return { user, session } when admin.
    5. Add optional seed: if process.env.ADMIN_EMAIL is set, upsert User with that email and set isAdmin=true. Document in seed.ts or README.
  </action>
  <verify>
    <automated>npx prisma validate</automated>
  </verify>
  <acceptance_criteria>
    - User model has isAdmin Boolean @default(false)
    - src/lib/auth/admin.ts exports requireAdmin
    - requireAdmin redirects to /login when no session
    - requireAdmin returns 403 when user.isAdmin is false (no redirect)
  </acceptance_criteria>
  <done>isAdmin field exists; requireAdmin helper enforces admin access</done>
</task>

<task type="auto">
  <name>Task 2: Admin panel shell at /admin</name>
  <files>src/app/admin/page.tsx, src/app/admin/layout.tsx</files>
  <read_first>
    - src/app/dashboard/page.tsx
    - src/lib/auth/admin.ts
    - src/app/layout.tsx
  </read_first>
  <action>
    1. Create src/app/admin/layout.tsx — server component that calls requireAdmin(); if it returns, render children. Layout wraps admin routes with admin-only guard.
    2. Create src/app/admin/page.tsx — server component; call requireAdmin(); render simple shell: "Painel Admin" heading, placeholder text "Monitoramento de VMs, logs e métricas em breve." Match dashboard styling: mx-auto max-w-4xl, Tailwind, indigo primary.
    3. Update src/components/NavBar.tsx: make it async, call getSessionFromCookies(); if session?.user?.isAdmin, render Link to /admin ("Admin") in the nav. Add NavBar to files_modified.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - /admin renders "Painel Admin" when user is admin
    - Unauthenticated access to /admin redirects to /login?redirect=/admin
    - Non-admin access returns 403
    - NavBar shows Admin link only for admin users
  </acceptance_criteria>
  <done>Admin panel shell accessible only by admins</done>
</task>

</tasks>

<verification>
- Visit /admin as unauthenticated → redirect to /login
- Visit /admin as non-admin → 403
- Visit /admin as admin (isAdmin=true) → see "Painel Admin"
</verification>

<success_criteria>
- ADMIN-01: Admin can log into internal admin panel with secure access controls
- SAFE-04: Admin routes protected with authentication and authorization
</success_criteria>

<output>
After completion, create .planning/phases/05-admin-operations-safety/05-01-SUMMARY.md
</output>
