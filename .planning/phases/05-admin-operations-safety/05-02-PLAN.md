---
phase: 05-admin-operations-safety
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - prisma/schema.prisma
  - prisma/migrations
  - src/lib/action-log.ts
  - src/lib/auth/admin.ts
  - src/app/api/admin/vms/route.ts
  - src/app/api/admin/vms/[id]/terminate/route.ts
  - src/app/admin/page.tsx
  - src/components/AdminVmList.tsx
autonomous: true
requirements:
  - ADMIN-02
  - ADMIN-03
  - SAFE-02
must_haves:
  truths:
    - Admin can view all active VMs with user email, profile, vultrInstanceId, readyAt, expiresAt, remaining time
    - Admin can terminate a VM and action propagates to Vultr and DB
    - Admin terminate action is logged with actorId, timestamps
  artifacts:
    - path: src/app/api/admin/vms/route.ts
      provides: GET admin VM list
    - path: src/app/api/admin/vms/[id]/terminate/route.ts
      provides: POST admin terminate
    - path: src/lib/action-log.ts
      provides: createActionLog helper
  key_links:
    - from: src/app/api/admin/vms/[id]/terminate/route.ts
      to: src/lib/vultr/client.ts
      via: deleteInstance call
      pattern: "deleteInstance"
    - from: src/app/api/admin/vms/[id]/terminate/route.ts
      to: src/lib/action-log.ts
      via: createActionLog after terminate
      pattern: "createActionLog"
---

<objective>
Add ActionLog model for audit trail, admin VM list API and UI, and admin terminate API that calls Vultr deleteInstance, updates ProvisionedVm, and logs the action.
</objective>

<execution_context>
@C:/Users/realg/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/realg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-admin-operations-safety/05-CONTEXT.md
@src/lib/auth/admin.ts
@src/lib/vultr/client.ts
@src/lib/provisioning/teardown.ts
@src/lib/provisioning/time-tracking.ts
@src/lib/provisioning/cost-safety.ts
@prisma/schema.prisma
</context>

<interfaces>
From src/lib/vultr/client.ts:
```typescript
export async function deleteInstance(instanceId: string): Promise<void>;
```

From src/lib/provisioning/time-tracking.ts:
```typescript
export function getRemainingMinutes(vm: Pick<ProvisionedVm, 'readyAt' | 'expiresAt'>): number | null;
```

From 05-CONTEXT.md ActionLog model:
- action: string (e.g. 'admin_terminate', 'vm_created', 'vm_destroyed')
- actorId: string (userId)
- actorType: 'user'|'admin'|'system'
- entityType: 'order'|'provisionedVm'|'payment'
- entityId: string
- metadata: Json
- createdAt: DateTime
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: ActionLog model and createActionLog helper</name>
  <files>prisma/schema.prisma, prisma/migrations, src/lib/action-log.ts</files>
  <read_first>
    - prisma/schema.prisma
    - .planning/phases/05-admin-operations-safety/05-CONTEXT.md
  </read_first>
  <action>
    1. Add ActionLog model to prisma/schema.prisma:
       - id String @id @default(cuid())
       - action String
       - actorId String?
       - actorType String (e.g. 'user'|'admin'|'system')
       - entityType String (e.g. 'order'|'provisionedVm'|'payment')
       - entityId String?
       - metadata Json?
       - createdAt DateTime @default(now())
       - @@index([entityType, entityId])
       - @@index([createdAt])
    2. Run `npx prisma migrate dev --name add_action_log`
    3. Create src/lib/action-log.ts with createActionLog(params: { action, actorId?, actorType, entityType, entityId?, metadata? }): Promise&lt;void&gt; — inserts into ActionLog. Use prisma from @/lib/db.
  </action>
  <verify>
    <automated>npx prisma validate</automated>
  </verify>
  <acceptance_criteria>
    - ActionLog model exists with action, actorId, actorType, entityType, entityId, metadata, createdAt
    - createActionLog inserts record and does not throw on success
  </acceptance_criteria>
  <done>ActionLog model and createActionLog helper ready for use</done>
</task>

<task type="auto">
  <name>Task 2: Admin VM list API and terminate API</name>
  <files>src/app/api/admin/vms/route.ts, src/app/api/admin/vms/[id]/terminate/route.ts</files>
  <read_first>
    - src/lib/auth/admin.ts
    - src/lib/vultr/client.ts
    - src/lib/provisioning/teardown.ts
    - src/lib/provisioning/time-tracking.ts
    - src/lib/action-log.ts
  </read_first>
  <action>
    1. Create GET src/app/api/admin/vms/route.ts:
       - Add getAdminFromRequest() to src/lib/auth/admin.ts: async (request?: Request) => { const session = await getSessionFromCookies(); if (!session) return { error: 401 }; if (!session.user.isAdmin) return { error: 403 }; return { user: session.user }; }. For Route Handlers, call it and if error return NextResponse.json({ error: 'Unauthorized' }, { status: 401 }) or 403.
       - Query: prisma.provisionedVm.findMany({ where: { status: { in: ['provisioning','vm_ready','expiring','destroying'] } }, include: { order: { include: { user: true } }, machineProfile: true }, orderBy: { createdAt: 'desc' } })
       - Map each vm to { id, status, vultrInstanceId, readyAt, expiresAt, userEmail: vm.order.user.email, machineProfileName: vm.machineProfile.name, remainingMinutes: getRemainingMinutes(vm) }
       - Return JSON array.
    2. Create POST src/app/api/admin/vms/[id]/terminate/route.ts:
       - getAdminFromRequest(); if not admin return 401/403.
       - Find ProvisionedVm by id; if not found return 404; if no vultrInstanceId return 400.
       - If status is destroyed/failed, return 400 (already terminated).
       - Call vultr.deleteInstance(vm.vultrInstanceId).
       - Update ProvisionedVm: status='destroyed', destroyedAt=new Date().
       - Update Order: status='completed'.
       - createActionLog({ action: 'admin_terminate', actorId: adminUser.id, actorType: 'admin', entityType: 'provisionedVm', entityId: vm.id, metadata: { vultrInstanceId: vm.vultrInstanceId } })
       - Return 200 { success: true }
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - GET /api/admin/vms returns array of VMs with userEmail, machineProfileName, remainingMinutes
    - POST /api/admin/vms/[id]/terminate calls deleteInstance, updates DB, creates ActionLog
    - Both routes return 401/403 when not admin
  </acceptance_criteria>
  <done>Admin VM list and terminate APIs functional</done>
</task>

<task type="auto">
  <name>Task 3: Admin VM list UI with terminate button</name>
  <files>src/app/admin/page.tsx, src/components/AdminVmList.tsx</files>
  <read_first>
    - src/app/admin/page.tsx
    - src/app/dashboard/page.tsx
    - src/components/DashboardVmList.tsx
  </read_first>
  <action>
    1. Create src/components/AdminVmList.tsx — client component that fetches GET /api/admin/vms, displays table: User, Profile, vultrInstanceId, Status, Ready At, Remaining, Actions. Use Tailwind table styling (rounded-lg border).
    2. For each row with status in vm_ready/expiring/provisioning/destroying and vultrInstanceId present: show "Terminar" button. On click: confirm (confirm()), POST /api/admin/vms/[id]/terminate, refetch list on success.
    3. Update src/app/admin/page.tsx: replace placeholder with AdminVmList. Add tabs or sections: "VMs Ativas" (AdminVmList), placeholder "Métricas" and "Logs" for Plan 3.
    4. Match dashboard styling: indigo primary, Tailwind, mx-auto max-w-6xl.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Admin page shows table of active VMs with user email, profile, status, remaining time
    - Terminar button calls terminate API and refreshes list
    - Placeholder sections for Métricas and Logs
  </acceptance_criteria>
  <done>Admin VM list UI with terminate control</done>
</task>

</tasks>

<verification>
- GET /api/admin/vms as admin returns VM list
- POST /api/admin/vms/[id]/terminate as admin destroys VM in Vultr and DB
- ActionLog contains admin_terminate record
</verification>

<success_criteria>
- ADMIN-02: Admin can view all active VMs with owning user, profile, start time, remaining time
- ADMIN-03: Admin can manually terminate VMs with actions reflected in provider and database
- SAFE-02: Admin terminate actions logged with timestamps and admin identifier
</success_criteria>

<output>
After completion, create .planning/phases/05-admin-operations-safety/05-02-SUMMARY.md
</output>
