---
phase: 05-admin-operations-safety
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified:
  - src/app/api/admin/health/route.ts
  - src/app/api/admin/logs/route.ts
  - src/lib/action-log.ts
  - src/lib/provisioning/service.ts
  - src/lib/provisioning/teardown.ts
  - src/app/admin/page.tsx
  - src/components/AdminHealthSection.tsx
  - src/components/AdminLogsSection.tsx
autonomous: true
requirements:
  - ADMIN-04
  - ADMIN-05
  - SAFE-01
  - SAFE-03
must_haves:
  truths:
    - Admin can view infra health: active VM count, per-profile counts, recent failures, rough cost
    - Admin can view logs of key events (payments, provisioning, destruction, admin overrides)
    - Basic abuse signals visible: repeated failed webhooks, provisioning errors, unusual restarts
    - SAFE-01 verified: one VM per user enforced (hasActiveOrder in Phase 2)
  artifacts:
    - path: src/app/api/admin/health/route.ts
      provides: Health metrics and abuse signals
    - path: src/app/api/admin/logs/route.ts
      provides: ActionLog and PaymentLog list
  key_links:
    - from: src/lib/provisioning/service.ts
      to: src/lib/action-log.ts
      via: createActionLog on VM ready
      pattern: "createActionLog"
    - from: src/lib/provisioning/teardown.ts
      to: src/lib/action-log.ts
      via: createActionLog on VM destroyed
      pattern: "createActionLog"
---

<objective>
Add admin health API (counts, failures, cost estimates, abuse signals), logs API (ActionLog + PaymentLog), wire createActionLog to provisioning and teardown, and build Admin health + logs UI sections.
</objective>

<execution_context>
@C:/Users/realg/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/realg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-admin-operations-safety/05-CONTEXT.md
@src/lib/auth/admin.ts
@src/lib/provisioning/cost-safety.ts
@src/lib/provisioning/teardown.ts
@src/lib/action-log.ts
@prisma/schema.prisma
</context>

<interfaces>
From 05-CONTEXT.md abuse signals:
- Repeated failed payment webhooks (same user/order): count from PaymentLog
- Repeated provisioning errors (same user): count by order.userId + failureCode
- Unusual restarts: count ProvisionedVm created per user in last 7d — flag if > 3

From src/lib/provisioning/cost-safety.ts:
```typescript
export async function getActiveVmCount(): Promise<number>;
```

PaymentLog: gatewayEventId, eventType, orderId, outcome, processedAt, metadata
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Wire createActionLog to provisioning and teardown</name>
  <files>src/lib/provisioning/service.ts, src/lib/provisioning/teardown.ts</files>
  <read_first>
    - src/lib/provisioning/service.ts
    - src/lib/provisioning/teardown.ts
    - src/lib/action-log.ts
  </read_first>
  <action>
    1. In src/lib/provisioning/service.ts: after VM is marked vm_ready (readyAt set), add createActionLog({ action: 'vm_created', actorType: 'system', entityType: 'provisionedVm', entityId: vm.id, metadata: { orderId, vultrInstanceId } }). Locate the exact place where status is set to vm_ready and readyAt is set.
    2. In src/lib/provisioning/teardown.ts: after VM is successfully destroyed (status=destroyed), add createActionLog({ action: 'vm_destroyed', actorType: 'system', entityType: 'provisionedVm', entityId: vm.id, metadata: { vultrInstanceId, orderId } }). Add before the destroyed++ increment.
    3. Ensure actorId is null for system actions.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - createActionLog called when VM becomes vm_ready in provisioning service
    - createActionLog called when VM is destroyed in teardown
    - actorType 'system' for both
  </acceptance_criteria>
  <done>VM created and destroyed events logged to ActionLog</done>
</task>

<task type="auto">
  <name>Task 2: Admin health and logs APIs</name>
  <files>src/app/api/admin/health/route.ts, src/app/api/admin/logs/route.ts</files>
  <read_first>
    - src/lib/auth/admin.ts
    - src/lib/provisioning/cost-safety.ts
    - prisma/schema.prisma
    - .planning/phases/05-admin-operations-safety/05-CONTEXT.md
  </read_first>
  <action>
    1. Create GET src/app/api/admin/health/route.ts:
       - getAdminFromRequest(); return 401/403 if not admin.
       - activeVmCount: getActiveVmCount()
       - perProfileCounts: prisma.provisionedVm.groupBy({ by: ['machineProfileId'], where: { status: { in: ['provisioning','vm_ready','expiring','destroying'] } }, _count: true }). Join with MachineProfile for name.
       - recentFailures: prisma.provisionedVm.findMany({ where: { status: 'failed', updatedAt: { gte: 24h ago } }, take: 20, include: { order: { include: { user: true } } } })
       - roughCostPerProfile: MachineProfile may not have hourlyRateCents; if not, return null or "—" per profile. Per CONTEXT: "placeholder '—' for MVP" if not available.
       - abuseSignals: (a) repeated failed webhooks: PaymentLog where outcome='canceled' or similar, group by orderId, count>2; (b) repeated provisioning errors: ProvisionedVm status=failed, group by order.userId, count>2; (c) unusual restarts: ProvisionedVm created in last 7d, group by order.userId, flag if count>3. Return { activeVmCount, perProfileCounts, recentFailures, abuseSignals }.
    2. Create GET src/app/api/admin/logs/route.ts:
       - getAdminFromRequest(); return 401/403 if not admin.
       - Query ActionLog with orderBy createdAt desc, take 100. Include optional ?entityType= and ?limit= query params.
       - Also return recent PaymentLog (outcome, orderId, processedAt) for payment events — or merge into unified log view. Per CONTEXT: "Centralize logs of key events (payments, provisioning, destruction, admin overrides)". ActionLog has admin/provisioning/destruction. PaymentLog has payments. Return { actionLogs: [...], paymentLogs: [...] } or unified format.
       - Return JSON: { actionLogs: ActionLog[], paymentLogs?: PaymentLog[] } with take 50 each.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - GET /api/admin/health returns activeVmCount, perProfileCounts, recentFailures, abuseSignals
    - GET /api/admin/logs returns actionLogs and paymentLogs
    - Both require admin
  </acceptance_criteria>
  <done>Health and logs APIs functional</done>
</task>

<task type="auto">
  <name>Task 3: Admin health and logs UI sections</name>
  <files>src/app/admin/page.tsx, src/components/AdminHealthSection.tsx, src/components/AdminLogsSection.tsx</files>
  <read_first>
    - src/app/admin/page.tsx
    - src/components/AdminVmList.tsx
  </read_first>
  <action>
    1. Create src/components/AdminHealthSection.tsx — client component, fetches GET /api/admin/health. Display: Total VMs ativas, Per-profile counts (table: Profile, Count), Recent failures (list with VM id, user, failureCode), Abuse signals (list: "X failed webhooks for order Y", "X provisioning errors for user Z", "User W: N VMs in 7d"). Use cards or simple sections. Tailwind, indigo accents.
    2. Create src/components/AdminLogsSection.tsx — client component, fetches GET /api/admin/logs. Display table: Timestamp, Action/Event, Actor, Entity, Details. Merge ActionLog and PaymentLog into single chronological view (sort by date). Pagination or "Load more" optional for MVP.
    3. Update src/app/admin/page.tsx: add AdminHealthSection and AdminLogsSection below AdminVmList. Use sections: "VMs Ativas", "Métricas e Saúde", "Logs". Remove placeholders from Plan 2.
    4. Add SAFE-01 verification note: Document in code or admin UI that "Um VM ativo por usuário" is enforced by hasActiveOrder in Phase 2. Optional: add a small "Conformidade" badge or tooltip in health section.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Admin page shows health section with counts, failures, abuse signals
    - Admin page shows logs section with ActionLog and PaymentLog events
    - SAFE-01 noted (one VM per user enforced)
  </acceptance_criteria>
  <done>Admin health and logs UI complete</done>
</task>

</tasks>

<verification>
- GET /api/admin/health returns metrics and abuse signals
- GET /api/admin/logs returns action and payment logs
- Provisioning and teardown create ActionLog entries
</verification>

<success_criteria>
- ADMIN-04: Admin can view basic infrastructure monitoring (counts, failures, cost)
- ADMIN-05: Admin can view logs of key events for compliance and debugging
- SAFE-01: One active VM per user (verified as enforced in Phase 2)
- SAFE-03: Basic suspicious-activity signals visible in admin views
</success_criteria>

<output>
After completion, create .planning/phases/05-admin-operations-safety/05-03-SUMMARY.md
</output>
